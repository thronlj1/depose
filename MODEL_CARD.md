# NMA 查询意图路由模型说明

## 1. 模型身份

| 项目 | 值 |
|---|---|
| 交付名称 | NMA Query Intent Router |
| 版本 | v1.16.0 |
| 模型 ID | `mdl_7fac9d9804e1` |
| 基础模型 | `intfloat/multilingual-e5-base` |
| 分类任务 | 单标签四分类 |
| 正式测试集 | Strict Test v3，固定 120 条 |
| Macro F1 | **0.8963** |
| Accuracy | **0.9000** |

该模型是 Chatbot 的查询路由器，不是通用问答模型。它只判断合法查询应该进入 RAG、单接口查询、多接口查询还是分析计算。Guard、身份与权限必须在模型外处理。

## 2. 推荐链路

```text
用户请求
  → Guard：过滤非法/越权表达
  → Token：获取 customer/admin、用户 ID、角色和数据范围
  → 本模型：四类查询路由
  → Planner：选择只读 API，并按实际工具数校正 single/multi
  → RAG 或只读 API
  → 响应生成
```

本模型不识别 customer/admin；该信息以外部 token 为准。模型也不应授权数据访问，最终权限必须由 API 服务校验。

## 3. 标签定义

| 标签 | 含义 | 示例 | 下游动作 |
|---|---|---|---|
| `knowledge_process` | 查询法规、规范、流程、字段含义、操作说明 | “许可续期需要什么材料？” | 进入 RAG/知识库 |
| `data_single` | 可由一个只读 API 直接返回的数据查询 | “查询申请 A123 的状态” | 调用一个查询接口 |
| `data_multi` | 需要多个只读 API 聚合，但不要求指标计算 | “给我申请详情、申请人资料和时间线” | 调用多个查询接口后合并 |
| `data_analytics` | 需要统计、比较、趋势、排名或确定性计算 | “比较本月各部门平均处理时长” | 多接口/报表接口 + 计算 |

`data_single` 与 `data_multi` 的最终区别是实际执行计划中的工具/API 数量。若用户文本无法体现后端接口拓扑，Planner 的结果比文本分类更可靠；生产环境可先把二者视为统一的 `data_query`，规划后再细分。

## 4. 模型结构

- E5 Base 编码器输出 768 维表示，最大输入长度 128 tokens。
- 对最后一层 token embedding 做 attention-mask mean pooling。
- 分类头：`Dropout → Linear(768,256) → GELU → Dropout → Linear(256,4)`。
- 推理使用 Softmax，取最高概率标签。
- 训练损失为单标签 Cross Entropy；不是多标签 BCE。
- 当前版本为 `head_only`：E5 全部冻结，只训练 MLP 分类头。元数据里的 `unfreeze_layers`/encoder LR 不代表本次实际解冻，`stage` 才是判断依据。
- 训练与推理都直接输入原始文本，部署时不要单独增加 `query:` 前缀，否则会产生训练/推理分布偏移。

## 5. 数据与评测

- 数据集共 1,000 条：800 train / 80 validation / 120 strict test。
- 训练集四类各 200 条，覆盖中文、英文和阿拉伯文。
- 测试集在训练、阈值选择和调参期间保持固定。
- Strict Test v3：Macro F1 0.8963，Accuracy 0.9000，共 12 条错误。

| 标签 | Recall | 测试样本 |
|---|---:|---:|
| `data_analytics` | 1.0000 | 30 |
| `data_multi` | 0.7143 | 28 |
| `data_single` | 0.9032 | 31 |
| `knowledge_process` | 0.9677 | 31 |

最明显问题是 `data_single ↔ data_multi`。96.19% 不是该版本在固定严格测试集上的正式 Macro F1，不应用作验收指标。

## 6. 已知限制与使用边界

- 当前 Macro F1 未达到 95%，尤其不能把低置信度的 single/multi 结果当成确定事实。
- 置信度不是经过校准的业务概率，不能单独用于安全或授权决策。
- 模型只处理查询路由，不允许直接调用写接口；审批、分配、发布、状态变更等操作不在此模型范围内。
- 新法规、新页面名称、新 API 组合和组织内部缩写可能造成分布漂移。
- 输入超过 128 tokens 会截断，长问题应先保留用户目标、对象和查询条件再分类。

## 7. E5 解冻实验与后续建议

2026-08-04 已从当前 v1.16.0 冻结基线进行最后一层解冻实验，固定使用同一份 Strict Test v3：

| 候选版本 | 解冻层数 | Encoder LR | Seed | Validation Macro F1 | Strict Test Macro F1 | 结论 |
|---|---:|---:|---:|---:|---:|---|
| v1.17.0 | 1 | `2e-6` | 42 | 0.9619 | 0.8943 | 未超过冻结基线 |
| v1.18.0 | 2 | `2e-6` | 43 | 0.9619 | 0.8943 | 未超过冻结基线 |
| v1.19.0 | 1 | `1e-5` | 45 | 0.9619 | 0.8943 | 未超过冻结基线 |
| v1.20.0 | 2 | `1e-5` | 45 | 0.9619 | 0.9025 | 提升 0.62 个百分点，未激活 |

两次一层解冻都比 v1.16.0 的 0.8963 低约 0.20 个百分点。两层解冻配合 `1e-5` 学习率虽达到 0.9025，但仅多判对 1/120 条，而且 `data_multi` 正确数由 20/28 降至 19/28，没有改善主要业务短板。因此所有微调候选均未激活，也未进入本交付包。验证集 0.9619 与严格测试集结果的差距也说明继续提高 validation 分数没有直接解决泛化问题。

后续若继续尝试，应至少用 3 个固定 seed 比较平均值与波动；只有严格测试集 Macro F1 稳定提升 3–5 个百分点才替换本版本。目前更应优先补充 single/multi 边界数据，并让 Planner 根据真实 API 数量校正路由。

## 8. 权重来源与许可

基础编码器来自 `intfloat/multilingual-e5-base`，上游模型卡标注为 MIT License。本交付包中的业务数据、分类头与内部标签定义按公司内部资产制度管理，不随上游许可自动对外开放。

上游模型卡：https://huggingface.co/intfloat/multilingual-e5-base
